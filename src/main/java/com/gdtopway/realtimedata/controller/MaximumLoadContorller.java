/*
package com.gdtopway.realtimedata.controller;

import com.gdtopway.realtimedata.domain.MaximumLoad;
import com.gdtopway.realtimedata.domain.State;
import com.gdtopway.realtimedata.service.MaximumLoadService;
import com.gdtopway.realtimedata.service.StateService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

@RequestMapping("/maximumLoad")
@RestController
public class MaximumLoadContorller {
  */
/*   @Autowired
    private MaximumLoadService maximumLoadService;

    @RequestMapping(value = "/maximumLoadInfo")
    @ResponseBody
    @Transactional
    public List<MaximumLoad> info() {
       Iterator<MaximumLoad> stateIterator= maximumLoadService.saveMaximumLoad();
        List<MaximumLoad> maximumLoadList = new ArrayList<MaximumLoad>();
        while (maximumLoadIterator.hasNext()) {
            maximumLoadList.add(maximumLoadIterator.next());
        }
        return maximumLoadList;
    }
    }*//*

}*/
